import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'G1_MuellerSilvan_Uebungen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
